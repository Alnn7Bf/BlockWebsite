import { CustomData } from "@/types/custom"

export const DEFAULT_CUSTOM_DATA: CustomData = {
    title: "Sitio bloqueado",
    description: "Este sitio ha sido bloqueado para ayudarte a mantener el enfoque.",
    redirect: ""
}